{% if is_section_header %}
<div class="section-break" id="{{ page_id }}">
    <h{{ level + 1 }} class="section-title">{{ title }}</h{{ level + 1 }}>
</div>
{% else %}
<article class="page-content" id="{{ page_id }}">
    {{ content }}
</article>
{% endif %}
